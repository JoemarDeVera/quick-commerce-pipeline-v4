name: 'quick_commerce_v4'
version: '1.0.0'
config-version: 2

profile: 'quick_commerce_v4'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  quick_commerce_v4:
    staging:
      +schema: staging
    marts:
      +schema: marts
